import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Create Account - OpSale',
  description: 'Create a OpSale account to start shopping, track orders, and earn loyalty rewards.',
};

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
  return children;
}
